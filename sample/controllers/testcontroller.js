with (Sgfd.Base) {
    var TestController = new Sgfd.Controller({
        metaName: 'TestController',
        index: function() {
            // A function to be used in views etc
            // Put it into 'controllers' folder of your app
        }
    });
};