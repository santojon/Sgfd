with (Sgfd.Base) {
    var TestService = new Sgfd.Service({
        metaName: 'TestService',
        index: function() {
            // A function to be used in controllers etc
            // Put it into 'services' folder of your app
        }
    });
};